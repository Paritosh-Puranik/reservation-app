import { SortableHeadersDirective } from './sortable-headers.directive';

describe('SortableHeadersDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableHeadersDirective();
    expect(directive).toBeTruthy();
  });
});
